import 'package:flutter/material.dart';

class LineWhite extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 1.5,
      color: Colors.white,
      margin: EdgeInsets.only(left: 20, right: 20, bottom: 30),
    );
  }
}
